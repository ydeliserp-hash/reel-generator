{
  "name": "ffmpeg-reel-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Microservicio HTTP que compone reels verticales 1080x1920 con FFmpeg a partir de una especificacion JSON. Disenado para correr como contenedor en EasyPanel y ser invocado desde n8n.",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "fetch-fonts": "bash scripts/fetch-fonts.sh"
  },
  "dependencies": {
    "express": "^4.21.2",
    "fluent-ffmpeg": "^2.1.3",
    "multer": "^1.4.5-lts.1",
    "pino": "^9.5.0",
    "pino-http": "^10.3.0"
  },
  "devDependencies": {}
}
